.DS_Store
.agents/
.venv/
# training outputs: checkpoints, adapters, tensorboard logs, merged CT2 dirs
finetune_persian/run*/
finetune_persian/models/
finetune_persian/base_model/
finetune_persian/__pycache__/
# node_modules anywhere in the repo (regenerable via npm install)
**/node_modules/
# hf_build_dataset.py's local output, if built outside Modal -- audio/dataset
# bulk data, not reproducible-input source (the pushed Hub dataset is)
finetune_persian/sm_data/
